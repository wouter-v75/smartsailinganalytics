export const metadata = {
  title: 'Squash Shots | SmartSailingAnalytics',
  description: 'Capture and process boat photos with rotation, cropping, and squashing',
};

export default function SquashShotsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
